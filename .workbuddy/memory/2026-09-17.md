# 2026-09-17

## 系统探针 UI 优化（srv-dashboard）
- 修复：favicon.svg 缺失（服务端有路由但文件不存在）→ 新建卫星图标并补 `<link rel="icon">`；canvas 固定 300px 拉伸模糊 → DPR 自适应 + resize 重绘；数字跳动 → tabular-nums
- 新增：曲线图例（CPU/内存/最近4分钟）、刷新倒计时（1s tick，暂停显示"已暂停"）、错误横幅（fetch 失败顶部红条，成功自动隐藏）、退出二次确认、动态页面标题（hostname）
- 浅色主题：CSS 变量双主题（body[data-theme="light"]），跟随系统 prefers-color-scheme + localStorage 持久化（sp_theme），登录页同步，主题切换后重绘 canvas 配色；防闪烁初始化脚本置于 head
- 进程表：下拉排序改为表头点击排序（同列再点切换升降序，▼▲ 指示），搜索加 150ms 防抖
- 资源迷你条：Docker 与进程表的 CPU%/内存% 列加 mini-bar 可视化（docker stats 字符串用 pctOf() 正则提取百分比）
- 验证：接口回归 21/21 通过；agent-browser 实测截图确认浅色/暗色主题、图例、倒计时、迷你条、排序箭头均正常
- 经验：agent-browser 跨 PowerShell 调用会丢会话状态（cookie/页面），登录→操作→截图必须串在同一条命令里；daemon 偶发重启后 snapshot 指向登录页属正常
